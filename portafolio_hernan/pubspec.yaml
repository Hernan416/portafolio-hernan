name: portafolio_hernan
description: Un portafolio personal en Flutter.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts:
  url_launcher:
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # --- AÑADE ESTAS LÍNEAS AQUÍ ---
  # Asegúrate de que la 'a' de 'assets:' esté al mismo nivel
  # de indentación que 'uses-material-design:'.
  assets:
    - assets/images/
  # --------------------------------